{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "find node_modules -mindepth 1 -maxdepth 1 -not -name '.cache' -exec rm -rf {} + 2>/dev/null; npm install --no-audit --no-fund && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
